# 2026-08-10 개발일지

## 오늘의 요약

Gemini 음성 분석 프롬프트에 화자 분리(Speaker Diarization) 지시를 추가했다. 기존 프롬프트는 "대화를 전사하세요"라고만 요청했기 때문에 AI 요약문에 "한 사람이 다른 사람에게"처럼 화자를 구분하지 않는 표현이 섞여 있었다. 이번 수정으로 Gemini가 통화 녹음을 분석할 때 "어르신: ...", "통화자: ..." 형식으로 화자를 구분해 전사하도록 명시했다. 화자 구분이 불확실한 줄은 "화자미상: ..."으로 표시하고, 전체 줄 대비 화자미상 비율이 30%를 초과하면 `speaker_split_ok: false`로 판정해 대시보드에 경고 배지를 표시한다.

기능을 새로 만든 것이 아니라 이미 있어야 할 프롬프트 요청이 누락된 것을 보완한 수정이다.

## 세션: Gemini 화자 분리 누락 보완

### 요청과 목표

- `docs/2026-08-05_기능발굴_변화추적.md` §10 "안티그래비티 지시"에 기록된 A 작업: 화자 분리 누락 수정
- Gemini가 분석 결과를 반환할 때 전사 텍스트에 화자 레이블이 없는 문제 해결
- DB 스키마 변경 없이 프롬프트 수정 + 타입 추가 + UI 경고 배지만으로 처리

### 구현 및 변경 내용

**`ANALYSIS_PROMPT` 항목 1 교체** — 단순 전사 요청 → 화자 구분 전사 지시:
- "어르신"과 "통화자(복지사·가족)" 두 화자로 구분
- 화자 판단 근거(존댓말 방향, 안부 묻는 쪽이 통화자) 명시
- 확신 불가 시 "화자미상: ..."으로 표시하고 추측 금지
- 항목 2 요약 지시도 "어르신", "통화자"로 지칭하도록 수정

**`AudioAnalysisResult` 타입 확장** — `speaker_split_ok: boolean` 필드 추가

**`analyzeAudio()` 반환값 계산 추가:**
```typescript
const lines = (parsed.transcript ?? "").split("\n").filter((l) => l.trim());
const unknownCount = lines.filter((l) => l.startsWith("화자미상:")).length;
const speaker_split_ok = lines.length === 0 || unknownCount / lines.length <= 0.3;
return { ...parsed, speaker_split_ok };
```

**`/api/recordings/analyze` route.ts** — `ai_summary` JSON에 `speaker_split_ok` 포함

**`calls-client.tsx`** — `parseAiSummary` 반환 타입에 `speaker_split_ok?: boolean` 추가, 전사 섹션에 `speaker_split_ok === false` 시 경고 배지 렌더링

### 문제와 해결 과정

- 발생한 오류 없음
- Gemini가 화자 분리를 지원하더라도 프롬프트에 명시적 요청이 없으면 구분하지 않는 동작 확인
- `speaker_split_ok`를 별도 DB 컬럼 없이 기존 `ai_summary` JSON 블롭에 포함시켜 마이그레이션 생략

### 기술적 의사결정

- **DB 컬럼 신설 vs JSON 블롭 포함**: `ai_summary` 컬럼이 이미 JSON을 저장하고 `calls-client.tsx`가 파싱 중이어서 JSON 내 추가 필드로 처리. 나중에 쿼리 필터가 필요해지면 컬럼으로 승격 가능
- **화자미상 임계값 30%**: "확신 불가" 줄이 소수라면 경고 미표시, 1/3 이상이면 신뢰 불가로 판단

### 테스트 및 검증

- `npx tsc --noEmit` — 에러 없음 (출력 없음 = 통과)
- 실제 녹음 파일 재분석은 기관 방문 시연 전 별도 진행 예정 (실제 오디오 파일 필요)

### 변경 파일

- `src/lib/silverlink/audio/audio-analyzer.ts`: 프롬프트 수정, 타입 확장, 화자분리 품질 계산 추가
- `src/app/api/recordings/analyze/route.ts`: `ai_summary`에 `speaker_split_ok` 포함
- `src/app/(protected)/dashboard/calls/calls-client.tsx`: `parseAiSummary` 타입 수정, 경고 배지 렌더링

### 미해결 사항 및 다음 작업

- 기존 녹음 3건을 재분析해 전사에 "어르신:" / "통화자:" 레이블이 실제로 나오는지 확인 필요 (실제 오디오 파일 접근 필요)
- 검증 후 `docs/00_제품_사실표.md`의 "화자 분리 미구현" 항목을 "구현됨"으로 이동

### 커밋 정보

- 커밋 메시지: fix: Gemini 분析 프롬프트에 화자 분리 지시 추가 + speaker_split_ok 판정
- 브랜치: main

---

## 세션: Slice 3 — AI 며느리 safety_alerts 연동

### 요청과 목표

AI 며느리에서 어르신이 "아까 넘어졌어" 같은 안전 우려 발언을 하면, SilverLink 가족 대시보드에도 즉시 알림이 올라가도록 연동한다.

### 구현 내용

**`docs/migration-v2-ai-myeoneuri-safety-bridge.sql`** (신규):
- `safety_alerts.call_id` — NOT NULL 제약 해제
- `safety_alerts.source` — TEXT 컬럼 신규 추가 (기본값 `'call_recording'`, CHECK: `'call_recording' | 'ai_myeoneuri'`)
- 기존 레코드는 `source = 'call_recording'`으로 자동 보호됨
- **Supabase 대시보드 SQL 에디터에서 직접 실행 필요 (supabase CLI 미사용)**

**`src/lib/supabase/safety-alerts-repo.ts`** (수정):
- `SafetyAlert.call_id`: `string` → `string | null`
- `SafetyAlert.source`: 신규 필드 `"call_recording" | "ai_myeoneuri"`
- `SafetyAlertInsert.call_id`: `string` → `string | null` (optional)
- `SafetyAlertInsert.source`: optional 필드 추가

### 기술적 의사결정

- **INSERT 실패 시 silently ignore**: `reportSafetyConcernToSilverLink`는 `.catch(() => {})` 패턴. 연동이 실패해도 어르신의 TTS 응답·화면 표시는 정상 진행된다. SilverLink 연결이 없거나 네트워크 오류여도 ai-myeoneuri 핵심 기능을 방해하지 않는 것이 원칙이다.
- **owner_user_id = 로그인한 보호자 UID**: AI 며느리에 로그인한 사람이 보호자이므로 `supabase.auth.getUser()`로 가져온 user.id를 그대로 사용. SilverLink RLS(`owner_user_id = auth.uid()`)와 일치한다.
- **call_id 없음**: AI 며느리 대화는 통화 녹음 기반이 아니므로 call_id = NULL. DB 마이그레이션으로 nullable로 변경.

### 테스트 및 검증

- `npx tsc --noEmit` — silverlink-web-input, ai-myeoneuri 모두 에러 없음
- DB 마이그레이션 미실행 상태 — Supabase SQL 에디터에서 실행 후 실기기 테스트 필요

### 변경 파일

- `docs/migration-v2-ai-myeoneuri-safety-bridge.sql` (신규)
- `src/lib/supabase/safety-alerts-repo.ts` (수정)

---

## 세션: SilverLink 서비스 근거 페이지에 AI 며느리 홍보 카드 추가

### 요청과 목표

SilverLink 앱 안에 AI 며느리를 자연스럽게 홍보해 다운로드를 유도한다.
로그인 강제 연동 방식 대신, SilverLink를 쓰는 케어워커·가족이 AI 며느리를 발견하는 유입 경로를 만드는 것이 목적이다.

### 구현 내용

`src/app/(protected)/dashboard/references/page.tsx`의 `<ReferenceAccordion />` 아래에 홍보 카드 추가.
- 보라색(violet) 그라디언트 카드로 기존 슬레이트 톤과 시각적으로 구분
- 앱 아이콘(👩‍👧), 앱 소개 문구, Play Store 링크 버튼 포함
- Play Store 패키지명: `com.leejanghan.aimyeoneuri`

### 기술적 의사결정

- **별도 페이지 신설 vs 기존 페이지에 카드 삽입**: 서비스 근거 페이지는 케어워커가 신뢰 정보를 확인하러 들어오는 곳. 같은 맥락에서 AI 며느리를 "SilverLink 패밀리 앱"으로 소개하면 설득력이 높다고 판단. 클릭 1회로 도달 가능.
- **수익 모델 연결**: SilverLink B2B 구독 고객이 AI 며느리 B2C 다운로드를 자연 유도 → 두 앱 간 교차 유통. 로그인 마찰 없이 스토어 링크 노출만으로 충분.

### 테스트 및 검증

- `npx tsc --noEmit` — 에러 없음

### 변경 파일

- `src/app/(protected)/dashboard/references/page.tsx`: AI 며느리 홍보 카드 추가

### 커밋 정보

- 커밋 메시지: feat: 서비스 근거 페이지에 AI 며느리 홍보 카드 추가
- 브랜치: main

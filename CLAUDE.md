@AGENTS.md
@docs/coding-guidelines.md

---

# 🎯 북극성 — 이 저장소의 유일한 목표

> **방문요양센터·주간보호센터 20곳에게 월 15만원 구독을 판다. (월 300만원)**

B2C(자녀 개인 구독)는 **메인이 아니다.** 개인에게 월 9,900원을 300가구 파는 것보다,
기관 20곳에 파는 것이 유통·객단가·유지비 모든 면에서 우월하다.
근거: 진단컨설팅 피드백(2026-07-16) "고객층 노령화 → B2B 전환", `docs/PRD-day26-caseworker-dashboard.md`.

## 🚨 제품 제1원칙 — 어르신은 아무것도 하지 않는다

> **어르신은 앱을 설치하지 않고, 화면을 보지 않고, 터치하지 않는다. 평소처럼 전화를 받기만 한다.**

이것이 이 제품의 유일한 해자다. 어르신에게 무언가를 요구하는 순간 제품은 죽는다.
어르신 측 UI·버튼·알림·설치를 전제로 하는 어떤 기능도 **설계 단계에서 거부**한다.

> **예외 — AI 며느리 (`ai-myeoneuri` 저장소):** AI 며느리는 어르신 본인이 폰에 설치해서 쓰는 앱이다. 이 제1원칙과 정면으로 충돌하는 것처럼 보이지만, 두 제품은 해자가 다르다. SilverLink의 해자는 "기관 담당자가 통화 기록 입력을 0으로 만드는 것"이고, AI 며느리의 해자는 "자녀가 부모 폰에 깔아주고 월 9,900원을 내는 스미싱·보이스피싱 방어"다. 같은 저장소가 아니고, 같은 판매 채널이 아니며, 어르신에게 기관 업무를 요구하지 않는다. AI 며느리 관련 작업은 `ai-myeoneuri/docs/PRD-B2C-1000subs.md`를 기준으로 판단하고, 이 제1원칙을 위반 사유로 거절하지 않는다. (ADR-017 참조)

- 앱을 설치하는 사람 = **전화를 거는 쪽**(사회복지사·생활지원사·자녀)
- 통화는 이미 매일 일어나고 있다 → 녹음 → AI 분석 → 보고서 자동 생성·발송
- 근거: 1차 MVP 실사용 피드백(2026-07) — "개개인이 굳이 다운받아 쓸 이유가 없다.
  복지사나 기관이 수십 명의 통화 기록을 올리고 AI 분석 결과를 보는 편이 낫다."

**경쟁 지형 (2026-07 확인):** 네이버 클로바 케어콜이 이미 전국 128~150개 시군구·약 5만 명을 점유.
클로바는 **AI가 어르신에게 전화를 건다**(로보콜). 우리는 **사람이 건 진짜 통화를 분석한다.**
→ 로보콜 영역으로 절대 들어가지 않는다. 정면충돌이며 이길 수 없다.

**핵심 판매 문장(모든 UI·문서·카피의 기준):**
> "생활지원사가 어르신 30명과 통화한 뒤 기록을 입력하는 시간을, 0으로 만듭니다."

**절대 하지 말 것:** 생활지원사에게 **또 하나의 입력 화면**을 주는 것.
그들은 이미 정부 지정 업무 시스템에 기록을 입력하고 있다. 우리의 가치는 입력을 늘리는 것이 아니라
**입력을 없애고, 붙여넣을 수 있는 결과물을 대신 만들어 주는 것**이다.

---

# 📌 현재 작업 기준 문서

> **`docs/PRD-v2-org-mvp.md` (2차 MVP · Day 34~40)가 지금의 유일한 작업 기준이다.**

작업 시작 전 해당 F 섹션의 **수용 기준**을 읽고, 만족하지 못하면 커밋하지 않는다.
이 PRD에 없는 기능 요청은 `docs/BACKLOG.md`로 보낸다.

**작업 순서 고정:** F1 기관 테넌시 → F2 대시보드 → **F6 데모(여기서 영업 개시)** → F3 배치 리포트 → F5 붙여넣기 → F4 자동발송 → F7 개인정보

---

# 🔒 기능 동결 (Feature Freeze)

**유료 고객 5곳을 확보하기 전까지 아래 3개 외의 신규 기능은 만들지 않는다.**

1. **기관용 대시보드** — 담당 어르신 수십 명의 상태를 한 화면에서 확인 (역할 분기: 개인 / 기관)
2. **AI 분석 보고서 자동 생성 + 정해진 시각 자동 발송** — 복지사 메일로 발송. 형식은 **기관 시스템에 그대로 붙여넣을 수 있는 형태**(CSV/표/텍스트 블록)
3. **랜딩 페이지** (`silverlink-ai.vercel.app`) — 현재 로그인 리다이렉트 상태, 기관 대상 소개 페이지로 교체

## 새 기능 요청이 들어왔을 때의 판단 규칙

- **유료(또는 파일럿) 기관이 직접 요구한 기능인가?** → 만든다.
- 아니면 → `docs/BACKLOG.md`에 적고 **만들지 않는다.** "있으면 좋을 것 같아서"는 거절 사유다.
- 리팩터링·기술 스택 교체는 **매출에 직접 기여하지 않으면 후순위**다.
**기존 화면 재디자인 금지. 신규 화면은 `docs/design-system.md` 를 따른다.**

이 프로젝트의 최대 실패 위험은 버그가 아니라 **기능을 계속 더 만드는 것**이다.

## 개인용 / 기관용 분리 원칙

- **저장소와 앱은 절대 분리하지 않는다.** 1인 개발자가 코드베이스 2개·스토어 심사 2개를 유지하는 순간 속도가 절반이 된다.
- 분리는 **로그인 후 역할(role) 분기**로만 한다. (`docs/PRD-day28-comprehensive-view-role-references.md` 기반)
  - `role: individual` → 기존 가족 대시보드
  - `role: org` → 기관 대시보드 + 다수 어르신 집계 + 보고서 자동 발송
- 브랜드·랜딩 페이지는 2개로 보여도 되지만, **코드는 하나**다. ("브랜드는 2개, 공장은 1개")

---

# 🔐 개인정보 — B2G/B2B 계약의 실제 관문

통화 녹음은 **민감정보**다. 기관 계약에서 기능보다 먼저 검증당하는 항목이므로 처음부터 지킨다.

- 녹음 원본은 **분석 후 즉시 파기**하는 것을 기본값으로 한다(보관은 옵트인).
- 어르신 **본인 동의 획득 절차**를 제품 안에 명시적으로 넣는다. 기관이 요구할 것이다.
- 데이터 보관 위치·암호화·접근 로그를 문서 한 장으로 항상 답할 수 있게 유지한다.
- 이 항목이 준비 안 되면 **아무리 좋은 데모도 계약으로 넘어가지 않는다.**

---

# 🚫 Play Store 재발 방지 규칙 (2026-07-24 3회 연속 거절)

앱 스토어 관련 작업 시 아래를 **반드시** 지킨다. 위반 시 심사가 다시 거절된다.

- **카테고리를 "의료(Medical)"로 설정하지 않는다.** → 개인 계정 배포 불가. 반드시 **생산성/라이프스타일**.
- **앱 콘텐츠 → 건강 앱 선언은 전 항목 해제 유지.** 수면·스트레스·질병관리·약물관리 등 어떤 것도 체크하지 않는다.
- **스토어 설명 문구에 "건강"이라는 단어를 쓰지 않는다.** 건강 선언 불일치로 거절된다.
  → 대체 표현: "돌봄", "가족 소통", "케어 인사이트", "생활 기록".
- **불필요한 권한을 선언하지 않는다.** `FOREGROUND_SERVICE_MEDIA_PLAYBACK`은 `tools:node="remove"`로 제거된 상태를 유지한다.
- `READ_CALL_LOG`는 제한 권한이므로, 검토 안내에 사용 목적 설명이 항상 포함되어야 한다.

---

# 👥 역할 분담 (중복 작업 금지)

| 담당 | 범위 |
|---|---|
| **이 저장소의 Claude Code (Antigravity)** | 앱·웹 **코드 구현**, 빌드, 배포, 마이그레이션, 테스트 |
| **Cowork 쪽 Claude** | 기획서·제안서·전자책 원고·리드 수집 스크립트·`CLAUDE.md` 관리 |

- 소스 코드(`src/`, `app/`, `components/`, `lib/`)는 **이 저장소의 Claude Code가 단독으로 소유**한다.
- 영업·마케팅 문서는 Cowork 쪽이 소유한다. 여기서 만들지 않는다.
- 서로 같은 파일을 동시에 고치지 않는다. 충돌 시 **코드는 Claude Code 우선, 문서는 Cowork 우선**.

---

## 커밋·푸시 전 개발일지 자동 갱신

- 사용자가 커밋 또는 푸시를 요청하면, 실제 `git commit`을 실행하기 전에 반드시 `docs/CLAUDE_COMMIT_WORKLOG_INSTRUCTIONS.md`를 읽고 따른다.
- 해당 지침에 따라 오늘 날짜의 `docs/work-log/YYYY-MM-DD.md`를 생성하거나 갱신한다.
- 같은 날짜의 파일이 이미 있으면 새 파일을 만들지 말고, 기존 내용과 중복되지 않게 이번 작업을 추가한다.
- 개발일지와 `README.md`의 작업일지 색인을 코드 변경과 같은 커밋에 포함한다.
- 개발일지 갱신이 실패하면 커밋·푸시를 진행하지 말고 실패 원인을 사용자에게 알린다.

### 개발일지는 자산이다

`docs/work-log/`는 단순 기록이 아니라 **전자책 《혼자서 AI 앱을 앱스토어에 올리기까지》의 원고**로 재사용된다.
따라서 일지에는 아래를 반드시 남긴다.

- **막혔던 지점과 그 원인** (해결책만 적지 말 것 — 독자가 사는 것은 시행착오다)
- **왜 그 기술적 판단을 했는가** (선택지 비교)
- 실제 에러 메시지·거절 사유 원문


